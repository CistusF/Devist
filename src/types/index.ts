export type project_types = {
    title: string;
    description: string;
}

export type post_types = {
    title: string;
    subTitle: string;
    postImage: string;
}

export type material_types = {
    name: string;
    date: string;
    type: string;
}